# No ANTHROPIC_API_KEY needed — ClaimBack calls the local `claude` CLI
# (Claude Code), which uses your Claude subscription auth on this machine.
#
# You only need this file if you want to override defaults:
FLASK_SECRET_KEY=change_this_to_a_random_secret
